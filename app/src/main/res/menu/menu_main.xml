<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".base.BaseActivity">

    <item
        android:id="@+id/action_recent"
        android:orderInCategory="200"
        android:title="@string/action_fave"
        android:icon="@drawable/ic_recent"
        android:onClick="showRecentActivity"
        app:showAsAction="ifRoom"
        />
    <item
        android:id="@+id/action_swap"
        android:orderInCategory="100"
        android:title="@string/action_swap"
        android:icon="@drawable/ic_swap_cities_white"
        android:onClick="swapCities"
        app:showAsAction="ifRoom"
        />
    <item
        android:id="@+id/action_settings"
        android:orderInCategory="300"
        android:title="@string/action_settings"
        app:showAsAction="never"
        />
</menu>
