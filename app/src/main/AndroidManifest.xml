<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.postnov.android.yaschedule">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="sensorPortrait"
            android:theme="@style/AppTheme.StatusBarTransparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".schedule.ScheduleActivity"
            android:screenOrientation="sensorPortrait" />
        <activity
            android:name=".search.SearchActivity"
            android:screenOrientation="sensorPortrait" />
        <activity
            android:name=".fave.FaveActivity"
            android:screenOrientation="sensorPortrait" />
        <activity android:name=".stations.StationsActivity"></activity>
    </application>

</manifest>